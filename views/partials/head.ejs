<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/style.css">
    <title><%= tabTitle %></title>
</head>
<body>
    <div class="container">
        <header>
          <nav class="navbar">
            <h1><a class="navbar-brand" href="/">Home</a></h1>
            <% if (currentUser) { %>
                <div>
                    <a class="button button-primary" href="<%= baseUrl %>/myjournal">My Journal</a>
                </div>
                <div>
                    <a class="button button-primary" href="<%= baseUrl %>/new">Add Entry</a>
                </div>
                <form action="search" method="get" class="d-flex" role="search">
                  <input class="form-control me-2" type="search" name="search" id="search" placeholder="Search" aria-label="Search" onkeydown="if (event.keyCode == 13) { this.form.submit(); return false; }">
                  <input class="btn btn-primary" type="submit" value="Search" />
                </form>
                <form action="/logout?_method=DELETE" method="post">
                    <input class="btn btn-primary" type="submit" value="Log Out" />
                </form>
            <% } else { %>
              <form action="search" method="get" class="d-flex" role="search">
                <input class="form-control me-2" type="search" name="search" id="search" placeholder="Search" aria-label="Search" onkeydown="if (event.keyCode == 13) { this.form.submit(); return false; }">
              </form>
              <a class="btn btn-primary" href="/login">Log In</a>
            <% } %>
          </nav>
        </header>